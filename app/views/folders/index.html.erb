<h1>Listing folders</h1>

<%= link_to 'New Folder', new_folder_path %><br/>
<!--
<table>
  <tr>
    <th>Cover</th>
    <th>Name</th>
    <th>Created at</th>
    <th>Updated at</th>
    <th>Accessible to</th>
    <th>Owner</th>
    <th></th>
    <th></th>
  </tr>
  -->
  <% @folders.each do |folder| %>
    <div class="folder">
      <div>
        <div class="cover">
          <%= link_to image_tag(folder.cover.url(:quadratic)), folder %>
        </div>
        <div class="new">
          <%= image_tag("new_64x64.png") %>
        </div>
        <div class="new"><span>New</span></div>
      </div>
      <div><%= link_to folder.name, folder %></div>
    </div>
    <!--
  <tr>
    <td><%= link_to image_tag(folder.cover.url(:quadratic)), folder %></td>
    <td><%= link_to folder.name, folder %></td>
    <td><%= folder.created_at %></td>
    <td><%= folder.updated_at %></td>
    <td><% for role in folder.roles %><%= link_to role.name, role_path(role) %> <% end %></td>
    <td><%= link_to folder.user.username, user_path(folder.user) %></td>
    <td><%= link_to 'Edit', edit_folder_path(folder) %></td>
    <td><%= link_to 'Destroy', folder, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
  -->
<% end %>
<!--</table>

<br />
-->
